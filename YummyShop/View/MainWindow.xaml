<Window x:Class="YummyShop.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:YummyShop.ViewModel.MainWindowFolder"
        xmlns:fontW="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:bh="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow"
        Name="MainWindowName"
        Height="500"
        Width="800"
        Style="{StaticResource MainWindowStyles}">

    <Window.DataContext>
        <viewModel:MainNavigationWindow />
    </Window.DataContext>

    <Border Background="{StaticResource GradientBrush11}"
            CornerRadius="10"
            BorderBrush="{StaticResource GradientBrush12}"
            BorderThickness="3">

        <bh:Interaction.Triggers>
            <bh:EventTrigger EventName="MouseLeftButtonDown">
                <bh:InvokeCommandAction Command="{Binding MoveWindowCommand}"
                                        CommandParameter="{Binding ElementName=MainWindowName}" />
            </bh:EventTrigger>
        </bh:Interaction.Triggers>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="10" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        Style="{StaticResource ButtonWinbdowTollBarStyles}"
                        Command="{Binding Path=WindowMinimizateCommand}"
                        CommandParameter="{Binding ElementName=MainWindowName}">
                    <fontW:IconImage Icon="WindowMinimize"
                                     Style="{StaticResource IconButtonStyles}"
                                     Width="14" />
                </Button>
                <Button Grid.Column="2"
                        Style="{StaticResource ButtonWinbdowTollBarStyles}"
                        Command="{Binding WindowCloseCommand}"
                        CommandParameter="{Binding ElementName=MainWindowName}">
                    <fontW:IconImage Icon="Xmark"
                                     Style="{StaticResource IconButtonStyles}"
                                     Width="12" />
                </Button>

            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1"
                      Margin="0,40,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="100" />
                    </Grid.RowDefinitions>

                    <Image Grid.Column="0"
                           Source="Resources/Image/logo_1.jpg"
                           Height="100" />

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="1"
                                   Style="{StaticResource AuthorizationTextBlock}"
                                   Text="Username" />
                        <TextBox Grid.Row="2"
                                 Style="{StaticResource TextBoxLoginStyles}" />
                        <TextBlock Grid.Row="3"
                                   Style="{StaticResource AuthorizationTextBlock}"
                                   Text="Password" />
                        <PasswordBox Grid.Row="4"
                                     Style="{StaticResource TextBoxPasswordStyles}" />
                    </Grid>

                    <Button Grid.Row="2"
                            Style="{StaticResource ButtonLoginStyles}"
                            Content="Login" />

                </Grid>

            </Grid>

        </Grid>

    </Border>

</Window>
